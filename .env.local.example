# Optional. Get a free key at https://portal.cdp.coinbase.com to use
# Coinbase's own RPC for Base mainnet instead of the public endpoint, and to
# unlock paymaster-sponsored transactions in OnchainKit. The app runs fine
# without this set — it falls back to a public Base RPC.
NEXT_PUBLIC_ONCHAINKIT_API_KEY=

# Optional. The deployed origin (no trailing slash), used to resolve
# absolute URLs for the OG image and social previews. Falls back to
# http://localhost:3000 for local dev.
NEXT_PUBLIC_SITE_URL=

# Required in production. Secret used to sign (HMAC) the stateless session
# cookie issued after Sign-In With Ethereum. Use a long random string. In
# development a built-in insecure default is used if this is unset.
SESSION_SECRET=

# Optional. Directory for the default file-backed server store (net-worth
# history, tracked positions, and the Pro waitlist). Defaults to ./.data.
DATA_DIR=

# Optional. Postgres connection string. When set, the server store uses
# Postgres (production-grade, multi-instance safe) instead of the file store.
# Apply migrations/001_init.sql to the database first.
DATABASE_URL=

# Required to run the scheduled daily net-worth snapshot job
# (/api/cron/snapshot). The job refuses to run if this is unset. Vercel Cron
# sends it automatically as a Bearer token when configured (see vercel.json).
CRON_SECRET=

# Optional (Phase 2 "Pro"). API key for the Zerion portfolio indexer. When set,
# signed-in Pro users get automatic, complete, multi-chain detection of every
# token and DeFi position (/api/portfolio) instead of the curated Base flow.
# Without it, the app stays on free public data. Get a key at
# https://developers.zerion.io.
ZERION_API_KEY=

# Optional. Comma-separated wallet addresses that always get Pro (comps / team
# / grandfathering), regardless of billing. When billing is NOT configured and
# this is unset, every signed-in user is treated as Pro (handy in dev).
PRO_ADDRESSES=

# Optional (Phase 3 billing). When set, Pro is sold via Stripe and entitlement
# is enforced from live subscription state.
#   STRIPE_SECRET_KEY     — server API key (sk_live_… / sk_test_…).
#   STRIPE_PRO_PRICE_ID   — the recurring Price ID for the Pro plan.
#   STRIPE_WEBHOOK_SECRET — signing secret for /api/billing/webhook.
# Apply migrations/002_subscriptions.sql to the database first.
STRIPE_SECRET_KEY=
STRIPE_PRO_PRICE_ID=
STRIPE_WEBHOOK_SECRET=

# Optional. Contact address shown on /support. Defaults to a placeholder —
# set this to a mailbox you actually monitor before launch.
NEXT_PUBLIC_SUPPORT_EMAIL=

# Optional (Phase 0 demand test). Your site's domain as registered in
# Plausible (e.g. basewealthtracker.app). When set, cookie-less,
# privacy-respecting page analytics load; when unset, no analytics at all.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=
