import { ComingSoonPage } from "@/components/nova/ComingSoonPage";

export default function SwapPage() {
  return (
    <ComingSoonPage
      title="Swap"
      description="Token swaps aren't wired up yet — NOVA stays read-only until a safe execution flow is in place."
    />
  );
}
